import 'package:accessnav_flutter/main.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('AccessNav app can be created', () {
    expect(const AccessNavApp(), isA<AccessNavApp>());
  });
}
